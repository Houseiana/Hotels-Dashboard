# ملاحظات على Hotel Management API

خلصت ربط لوحة تحكم الفنادق على الـ API. كل الشاشات شغالة على السيرفر دلوقتي — الفنادق، إضافة وتعديل فندق، الأسعار والإتاحة، الحجوزات، التقييمات، الإعدادات، وكمان الخدمات وقواعد الإقامة وسياسة الأطفال.

جمّعت هنا كل حاجة وقفت قدامي وأنا بشتغل. رتبتها بالأولوية عندي، مش بالترتيب اللي لقيتها بيه.

اللي جربته كان على:
`https://houseiana-api.jollyisland-881a1746.eastus.azurecontainerapps.io`
بحساب `user@admin.com`.

---

## أهم حاجة: تقويم الأسعار مالوش سنة

```
GET /api/room-types/{id}/calendar?month=1   →  "year": 2026
GET /api/room-types/{id}/calendar?month=12  →  "year": 2026
```

مفيش بارامتر `year` خالص، والـ endpoint بيرد بالسنة الحالية أيًا كان الشهر اللي تطلبه.

يعني المالك **مش قادر يسعّر 2027**. والفنادق بتفتح أسعار الموسم الجاي من دلوقتي، مش في يناير.

أنا مؤقتًا قفلت التنقل بين الشهور عند حدود السنة، عشان لو حد ضغط "الشهر اللي جاي" من ديسمبر ميجيلوش داتا يناير 2026 وهو فاكر إنها يناير 2027. بس ده حل عرج، الحل الصح إن الـ endpoint ياخد `year`.

---

## الحجوزات

### كل الحجوزات في نداء واحد — اتعمل، شكرًا

كنت طالب `GET /api/hotels/bookings` بفلتر `hotelId` اختياري بدل ما يكون إجباري في الـ path. لقيته اتنفّذ:

```
GET /api/hotels/bookings
    ?hotelId= &search= &statusId= &fromDate= &toDate= &page= &limit=
```

جربته: بيشتغل من غير `hotelId` (كل الفنادق)، وبيه (فندق واحد)، والفلترة بالحالة تمام. شِلت الدمج اللي كان في المتصفح والبانر اللي كان بيحذّر منه — البحث والفلترة والترقيم كلهم بقوا على السيرفر دلوقتي.

**بس ملحوظة على طريقة النشر:** الـ endpoint القديم `/api/hotels/{hotelId}/bookings` **اتشال في نفس اللحظة**. يعني شاشة الحجوزات عندي فضلت مكسورة (404 على كل نداء) من ساعة النشر لحد ما أنا لاحظت وعدّلت.

لو الـ endpoint القديم فضل شغال أسبوع كمان جنب الجديد، كان الانتقال هيعدي من غير ما حد يحس.

**وحصلت تلات مرات دلوقتي:** الحجوزات، وبعدها الرسوم، وبعدها قايمة المرافق. في كل مرة الشاشة بتفضل مكسورة لحد ما ألاحظ بالصدفة وأنا بشتغل على حاجة تانية.

مش بشتكي من التغييرات نفسها — كلها كانت تحسينات وأنا طلبتها. بشتكي إني بعرف بيها من 404 مش من رسالة. **رسالة قبل النشر بيوم تكفي.**

### ومفيش تغيير حالة

دورت في الـ 34 endpoint بتوع HotelManagement كلها ومفيش ولا واحد بيغيّر حالة حجز. لا تأكيد، لا إلغاء، لا تسجيل دخول أو خروج.

فشِلت زراير "تأكيد" و"إلغاء" من الشاشة وحطيت مكانها سطر بيقول إن الحجوزات للعرض بس. مش عايز أسيب زراير المستخدم يدوس عليها وميحصلش حاجة.

فيه `BookingStatus` lookup بـ 11 حالة، فالحالات موجودة في الداتابيز — ناقص بس الـ endpoint اللي يغيّرها.

---

## `account/edit` مش بيقبل تفريغ خانة

```
POST /api/hotels/account/edit
{"name": null,  "contactEmail":"user@admin.com", "contactPhone":null, "currencyId":2}   → 200
{"name": "",    ...}                                                                     → 200
```

الاتنين بيرجعوا 200 والقيمة القديمة بتفضل زي ما هي.

يعني بعد ما المستخدم يكتب حاجة في خانة، **مش قادر يمسحها تاني**. لازم يحطّ قيمة تانية مكانها.

حصلت معايا فعلًا: اسم الحساب كان فاضي، جربت الحفظ من الشاشة فاتكتب فيه اسم اختبار، وبعد كده معرفتش أرجّعه فاضي.

المفروض `null` تمسح والـ field اللي مش مبعوت أصلًا هو اللي يتساب زي ما هو.

---

## أسماء المدن والدول — اتعملت، شكرًا

كنت طالب `cityName` و `countryName` في رد التفاصيل. اتضافوا، وكمان `stateName`.

شِلت الالتفافة اللي كانت بتنده الـ list وتدوّر على الفندق بالاسم عشان تجيب اسم مدينته. نداء أقل كل مرة حد يفتح صفحة تعديل.

**لسه ناقص:** `stateId` و `countryId`. الأسماء بتتعرض تمام، بس قوائم الاختيار (دولة ← محافظة ← مدينة) مش قادرة تفتح على الاختيار الحالي من غير الـ ids. المالك بيفتح التعديل فيلاقي خانة الدولة فاضية رغم إن الفندق ليه دولة.

---

## مقياس التقييم — اتظبط

كنت سائل `ratingValue` من ٥ ولا من ١٠. الإجابة **من ١ لـ ٥**.

اللوحة كانت بتعرضها من ١٠ (زي بوكينج)، يعني ضيف قيّم ٤ من ٥ كان هيبان **٤ من ١٠** بلون أحمر وكلمة "ضعيف" جنبه.

غيّرت العرض كله عندي لـ ١–٥. مش محتاج حاجة منكم هنا، كاتبها بس عشان تبقوا عارفين إن الرقم ده كان غلط في اللوحة لفترة، ولو حد شافها قبل كده وقال إن التقييمات واطية — دي كانت السبب.

---

## حاجات أصغر

**صورة الغلاف**
`coverPhoto` في `edit-hotel` بتاخد ملف مرفوع بس. مفيش طريقة أقول "خلي الصورة رقم 3 اللي عندك أصلًا هي الغلاف". فلو المالك رتّب صوره وحطّ صورة قديمة الأول، الغلاف بيفضل زي ما هو. حطيت تنبيه للمستخدم إن ده حصل، بس الأحسن يكون فيه `coverPhotoId`.

**الإنشاء مبيرجعش id**
`POST /api/hotels` بترجّع نجاح من غير id الفندق الجديد. فبعد الإنشاء بضطر أنده الـ list وأدوّر بالاسم عشان أعرف أوديه فين. لو الاسم مكرر ممكن أجيب الغلط. نفس الحكاية في `POST /api/hotels/{id}/fees` (بترجّع `data: true`).

**الفنادق المحذوفة بترجّع 404 على الحجوزات**
```
GET /api/hotels/911ebe02-.../bookings  →  404 {"message":"Hotel not found."}
```
الفندق ده حالته `Deleted` وبيظهر عادي في `GET /api/hotels`. مش شاكي من ده بالذات — منطقي إن المحذوف مالوش حجوزات — بس خلاني أفلتر المحذوفة قبل ما أنده. حابب أعرف لو ده مقصود ولا لأ.

**الرسوم رجعت كخدمات — وأحسن**

كنت سائل: الرسوم اتشالت مقصود؟ الإجابة اتضحت — مااتشالتش، **اتبدلت**.

```
راح:  /api/hotels/{id}/fees        + HotelFeeType (13)
جه:   /api/hotels/{id}/services     + HotelServices (21)
      /api/room-types/{id}/services + RoomServices (7)
```

ده أنضف بكتير. بقى فيه فرق واضح بين «الفندق عنده مطعم» (مرفق) و«خدمة الغرف بكام» (خدمة بسعر)، وخدمات الفندق اتفصلت عن خدمات الغرفة. و `Extra Bed` و `Baby Cot` بقوا في خدمات الغرفة — وده بيحل التناقض اللي كنت كاتبه تحت.

**حاجة واحدة راحت:** القايمة القديمة كان فيها `Other` مع `customName` عشان تكتب خدمة من عندك. الجديدة مقفولة على 21+7 ومفيش خانة حرة. فندق بيقدّم حاجة مش في القايمة مالوش مكان يكتبها.


**`/delete` و `/activation` بيعملوا toggle مش set**

دي لقيتها بالصدفة وأنا بمسح فنادق تجريبية. نديت `/delete` على فندق حالته `Deleted` أصلًا:

```
النداء الأول  → {"message": "Hotel deleted.",  "data": true}
النداء التاني → {"message": "Hotel restored.", "data": false}
```

نفس الحكاية في `/activation` (`activated` ↔ `deactivated`).

المشكلة إن الاسم بيقول حاجة والسلوك بيعمل حاجة تانية. زرار "حذف" عندي كان بينده `/delete`، والفندق المحذوف بيفضل ظاهر في القايمة — يعني ضغطة تانية كانت هترجّعه وهو مكتوب عليه "حذف".

ظبطتها عندي (زرار الحذف بيتحول لـ "استرجاع" على الفندق المحذوف)، بس الأنضف عندكم إما `/delete` تحذف بس ويكون فيه `/restore` منفصل، أو الاسم يبقى `/toggle-status` عشان يقول الحقيقة.

**نفس القيمة بترجع باسمين مختلفين**

```
GET /api/HotelManagementLookup/ChildPricingMode
  →  "Percentage Discount"                  (بمسافة)

GET /api/hotels/{id}
  →  "pricingMode": "PercentageDiscount"    (من غير مسافة)
```

أنا بحفظ رقم (`pricingMode: 3`) والفندق بيرجّعلي اسم. عشان أعرف الاسم ده أنهي اختيار في القايمة لازم أطابقهم — والمطابقة الحرفية بتفشل، والفشل معناه إني أقع على القيمة الافتراضية: `Free`.

يعني: مالك يحط خصم 50% لطفل، يتحفظ صح، يفتح التعديل يلاقيها "مجانًا"، يحفظ أي حاجة تانية — **الخصم يروح فعلًا من السيرفر**. وماحدش هيلاحظ غير لما ضيف يحجز بطفل ومايتحاسبش.

عاملها عندي مطابقة بتتجاهل المسافات فالاتنين شغالين، بس ده بيداري المشكلة مش بيحلها.

ومش حالة وحيدة: `bedType` بيرجع مرة اسم ومرة رقم، `services` بتاخد `serviceId` وترجّع `id`، و `policies` بتاخد `policyTypeId`. كل حقل مربوط بقايمة له اتجاهين مختلفين. الأنضف إن الرد يرجّع **نفس الرقم** اللي بياخده.


**القوايم مفيهاش عربي**
كل الـ lookups بترجّع `name` بالإنجليزي بس، مفيش `nameAr`. أنا عامل جدول ترجمة عندي للحاجات الثابتة (أنواع الغرف، الإطلالات، أنظمة الإقامة، المرافق، أنواع الرسوم)، بس **أسماء المدن والدول والقرى مقدرش أترجمها** لأنها بيانات مش قوايم ثابتة. فالمستخدم العربي بيشوف اسم مدينته بالإنجليزي.

**أنواع الأسرّة قلّت من 6 لـ 3**

خليني أوضّح الأول عشان مايحصلش لبس: أنا بتكلم عن `BedType` (السرير اللي جوه الغرفة)، **مش** `RoomCategory` (فئة الغرفة). فئات الغرف تمام، 8 وكلهم منطقيين.

المشكلة في الأسرّة:

```
BedType (3):
  1. King Bed
  2. Queen Bed
  3. Double Bed
```

تلاتتهم **نفس الحاجة** — سرير واحد كبير لشخصين، الفرق بينهم الحجم بس. يعني القايمة كلها بتقول جملة واحدة.

اللي مش موجود ومحتاجينه:

```
Twin Beds        ← أهم واحد
Single Bed
Sofa Bed
Bunk Bed
Extra Bed / Rollaway
Baby Cot / Crib
```

`Twin Beds` هو اللي بيوجعني. غرفة بسريرين منفصلين — وده نص فنادق الأعمال — مالهاش تمثيل خالص دلوقتي. المالك هيفتح القايمة، مش هيلاقي الاختيار، هيحط `Double Bed` لأنه أقرب حاجة، والضيف هيحجز وهو فاكر إن فيه سريرين ويوصل يلاقي سرير واحد. دي مشكلة عند الاستقبال مش مشكلة في اللوحة.

وحاجة غريبة لاحظتها: `Baby Cot` و `Extra Bed` **موجودين في `HotelFeeType`** كخدمات بفلوس. يعني تقدر تحاسب الضيف على سرير أطفال، بس مش قادر تقول إن الغرفة فيها واحد.

كانت فيها Twin و Sofa Bed و Bunk Bed قبل كده. اتشالوا مقصود ولا حصل حاجة في migration؟

**قايمة المرافق — اتظبطت، شكرًا**

كتبت هنا إن الـ 39 مرفق كانوا مكتوبين لشقق مفروشة وإن فندق 5 نجوم مش هيلاقي إنه يقول عنده مطعم. القايمة اتغيّرت كلها:

```
المرافق (17):  Swimming Pool · Gym · Spa · Restaurant · Café · Free WiFi
               Parking · Elevator · Business Center · Meeting & Event Rooms
               Kids' Play Area · Prayer Room · Garden/Terrace · Beach Access
               Wheelchair Accessible · Bar/Lounge · Banquet Hall

وقايمة منفصلة لمرافق الغرف (16): تكييف · تلفزيون · ميني بار · خزنة · بانيو…
```

**مصلى** إضافة ذكية ماكانتش في بالي.

وفصل مرافق الغرفة عن مرافق الفندق صلّح باگ عندنا: منتقي مرافق الغرفة كان بيقرا من قايمة الفندق، يعني المالك كان يقدر يقول إن **الغرفة فيها مطعم وقاعة احتفالات**.

**بس نفس ملاحظة النشر:** الـ 39 اتشالوا في نفس اللحظة، فجدول الترجمة عندي بقى 32 اسم منه مالوش وجود. لاحظتها بالصدفة وأنا بفحص حاجة تانية.


---

## اللي شغال تمام

عشان الكلام ميبقاش كله شكوى، دي الحاجات اللي جربتها من الأول للآخر وطلعت سليمة:

- إنشاء فندق كامل بـ multipart (غرف + أسرّة + خطط أسعار + مرافق + صور)
- تعديل فندق بالفرق: `edit-hotel` + `room-types/create|edit|delete` + `rate-plans/create|edit|delete`. الأسرّة بتتطابق صح بالـ `bedId` والعدد بيتحدّث من غير ما يتعمل سرير مكرر
- التقويم بيعكس التغييرات فورًا: `special-prices` بيرجع `isSpecialPrice: true`، و `inventory/block` بيقلل `available` صح، و `units: 0` بيحرر التواريخ
- `GET /api/hotels/overview` بيرجّع بالظبط الأرقام اللي الشاشة محتاجاها، وأهم حاجة إنه بيقول العملة (`revenueCurrencyCode`) فمش بجمع عملات مختلفة على بعض
- فلترة وترتيب التقييمات على السيرفر شغالة صح (`replyStatusId` و `sortId`)
- الخدمات: إنشاء وقراءة وحذف، على مستوى الفندق وعلى مستوى نوع الغرفة
- **السياسات والخدمات وسياسة الأطفال** — التلاتة بنفس النمط: بتتبعت مع الإنشاء، وليها endpoint منفصل للتعديل. نمط مريح ومتسق
- **`children-policy` بيتحقق من المدخلات بجدية** — تداخل الشرائح، المدى العمري، وإن `Free` و `As Adult` مايجيش معاهم قيمة. نقلت نفس القواعد للواجهة عشان المالك يشوف الغلط جنب الخانة بدل رسالة واحدة على السياسة كلها
- **العربي بيتخزن ويرجع سليم 100%**. جربته أكتر من مرة (`تجهيز البلكونة`, `فندق سيسايد جراند`) ومفيش أي مشكلة encoding

وحاجة كويسة لاحظتها: `managerId` اتشال من `POST /api/hotels` و `GET /api/hotels` وبقى الاعتماد على التوكن. ده كان ثغرة صلاحيات (أي حد كان يقدر يبعت أي `managerId`) وخلاص اتقفلت. شكرًا.

---

## اللي محتاجه بالترتيب

1. `year` في `calendar` — ده اللي واقف قدامي فعليًا، ومحدش يقدر يسعّر 2027 من غيره
2. endpoint لتغيير حالة الحجز (تأكيد / إلغاء / دخول / خروج) — الشاشة قراءة فقط من غيره
3. `account/edit` يقبل `null` كمسح للخانة
4. `stateId` و `countryId` في تفاصيل الفندق — الأسماء اتضافت، الـ ids لسه ناقصة
5. توحيد أسماء القوائم بين الـ lookup ورد الفندق، أو رجّعوا الأرقام
6. `BedType` قلّت من 6 لـ 3 — ناقصها `Twin Beds` وده بيكسّر غرف الأعمال
7. خانة حرة في الخدمات زي `Other` اللي كانت في الرسوم

والأهم من كل ده: **خبّروني قبل ما تشيلوا endpoint**.

أي حاجة مش واضحة قوللي وأنا أبعت الـ request والـ response بالظبط.
